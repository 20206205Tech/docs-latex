\newpage

{\centering \section*{KẾT LUẬN}}
\addcontentsline{toc}{section}{KẾT LUẬN}

\subsection*{Kết luận}

Sau thời gian nghiên cứu,
thiết kế và phát triển,
đồ án tốt nghiệp
đã hoàn thành đầy đủ các mục tiêu đề ra ban đầu,
đáp ứng toàn bộ các yêu cầu kỹ thuật
và nghiệp vụ được giao.

Về kết quả đạt được, đồ án đã mang lại các giá trị cụ thể như sau:

\begin{itemize}
\item \textbf{Về mặt sản phẩm:}
Xây dựng thành công một hệ thống trợ lý pháp luật hoàn chỉnh
hoạt động dựa trên kiến trúc vi dịch vụ
ổn định và dễ mở rộng.
Trợ lý ảo AI sử dụng giải pháp Agentic RAG tiên tiến với LangGraph,
cho phép trả lời các truy vấn pháp lý tiếng Việt một cách chính xác,
minh bạch qua luồng suy luận rõ ràng và dẫn nguồn từ CSDL pháp điển cũng như văn bản quy phạm pháp luật chính thống.
Đồng thời, hệ thống hỗ trợ đàm thoại giọng nói thời gian thực, xử lý tài liệu người dùng và tự động hóa quy trình thanh toán.

\item \textbf{Về mặt công nghệ:}
Tích hợp nhiều công nghệ hiện đại bao gồm
% data load tool
% Agentic RAG nâng cao sử dụng LangGraph.
% Qdrant Vector Database,

database postgres
s3 => su, r2

% (Microservice Architecture),

% ứng dụng phương pháp Thiết kế hướng miền (Domain-Driven Design - DDD) và các dịch vụ được
% điều phối qua Kong API Gateway.

% gRPC, RabbitMQ, Apache Kafka,
% gRPC cho trò chuyện trực
% tiếp, giao tiếp bất đồng bộ giữa các dịch vụ NestJS và Python được xử lý thông qua RabbitMQ.

% Luồng nghiệp vụ quy trình thanh toán thuê bao VIP qua các cổng nội địa (VNPay, MoMo, ZaloPay,
% SePay), được đồng bộ bằng nền tảng sự kiện Kafka.

% NestJS, Python (, FastAPI),

% hạ tầng tự động hóa CI/CD, Docker và Kubernetes được quản lý qua GitOps (ArgoCD).

% Toàn bộ vòng đời ứng dụng được
% tự động hóa thông qua CI/CD trên GitHub Actions, đóng gói bằng Docker và triển khai vận hành
% trên cụm Kubernetes thông qua ArgoCD.

% các tư vấn pháp lý với các bước suy
% luận logic và trích dẫn nguồn văn bản rõ ràng. Bên cạnh đó, ứng dụng còn mang đến trải nghiệm
% đàm thoại thời gian thực bằng cách tích hợp LiveKit cùng các mô hình xử lý giọng nói tiên tiến
% như Whisper (Speech-to-Text), Edge, Piper và ElevenLabs (Text-to-Speech).

Doppler
\subsubsection{Quản lý các thông tin bí mật (doppler)}

\item \textbf{Về mặt kiến thức và kỹ năng tích lũy:}
Qua quá trình thực hiện đồ án,
% sinh viên đã tích lũy được nhiều kiến thức chuyên sâu về xử lý ngôn ngữ tự nhiên (NLP), mô hình ngôn ngữ lớn (LLM),
% kiến trúc hệ thống phân tán và quy trình vận hành DevOps/GitOps.
% Đồng thời, sinh viên đã phát triển và nâng cao các kỹ năng quan trọng như:
% kỹ năng nghiên cứu khoa học,
% tự tìm kiếm và tổng hợp tài liệu chuyên ngành,
% kỹ năng lập kế hoạch và quản lý dự án, kỹ năng chế bản tài liệu chuyên nghiệp bằng LaTeX, cũng như kỹ năng viết báo cáo kỹ thuật.
\end{itemize}

Về mặt đánh giá,
hệ thống cho thấy tính thực tiễn cao khi giải quyết được bài toán tra cứu thông tin pháp luật phức tạp.
Tuy nhiên, tính chính xác của câu trả lời phụ thuộc nhiều
vào chất lượng và độ cập nhật của CSDL nguồn.
Các mô hình AI vẫn đóng vai trò
là một trợ lý hỗ trợ đắc lực và không thể thay thế hoàn toàn vai trò của các chuyên gia pháp lý trong các tình huống quyết định mang tính pháp lý cao.

\subsection*{Hướng phát triển}

Để hoàn thiện ứng dụng
và nâng cao khả năng
phục vụ người dùng trong thực tế,
hướng phát triển của đồ án
trong tương lai tập trung vào các nội dung sau:

\newpage

% Viết tắt
% Tài liệu tham khảo
% Viết hoa
% <!-- -->
% ELK

% % % Xây dựng data pipeline về bộ dữ liệu văn bản pháp luật Việt Nam.

% % % Xây dựng hệ thống microservice cho hệ thống chatbot

% % % LLM ( tinh chỉnh, ragas ....=> chọn model, cấu hình)

% % % RAG
% % % MICROSERVICE
% % % Domain Driven Design

% % % - [x] Xem kỹ về DDD và Viết test

% % % Data pipeline (air floww)

% % % - [x] Tạo vbplnew và vbplnew service
% % % - [x] Tạo pháp điển và pháp điển service

% % % - [x] Tạo gói common: 20206205tech-nestjs-common
% % % - [ ] Vẽ sơ đồ có gói common
% % % Gói auth python, nestjs vì auth service supabse => tiện, không lặp code

% % % auth: TOTP mfa
% % % <!-- HUST => không google: Đăng ký, email xác nhận, Đăng nhập, quên mật khẩu -->

% % % pay (xuất hóa đơn, event scing)

% % % <!-- Stripe -->
% % % tây nước ngoài có nhu cầu
% % % <!-- payment trùng 2 lần -->

% % % <!-- Không có chức năng giảm giá => Nhiều người mua cùng lúc -->

% % % SAGA

% % % Event scouicng

% % % per (GPY Onivoi)
% % % âm thanh giao diện sóng âm
% % % <!-- Sóng âm (waveform animation) -->

% % % <!-- sử dụng thuật toán LRU (Least Recently Used) -->

% % % doc
% % % - [ ] Xử lý các định dạng file khác nhau => RAG

% % % Khi chia doc service => doc nhẹ GET thoải mái và doc không cần AI nữa

% % % chat thêm
% % % <!-- feedback (0, 1) KHÓ vì microservice -->
% % % <!-- Phân trang cho tất cả API và giao diện (hàm phân trang chung) -->
% % % Do cần thời gian phản hồi nhanh cả gRPC và voice: tôi muốn chủ đề topic = null. Sau đó sẽ được xử lý sau?

% % % Nhiều giấy tờ văn bản khó tiếp cận

% % % người già cần voice
% % % người trẻ cần voice

% % % test playwwringt

% % % Github action=> tele telegram

% % % <!-- Test API test AI Ragas -->
% % % <!-- RAGAs chịu -->

% % % <!-- test ai, api, automation, ... -->
% % % <!-- Quay video demo -->

% % % kubernetes nhiều cụm, môi trương f đấm mây

% % % % \begin{itemize}
% % % % \item \textbf{Nâng cao chất lượng AI và tối ưu hóa RAG:} Nghiên cứu huấn luyện tinh chỉnh (Fine-tuning) mô hình nhúng (Embedding) trên tập dữ liệu luật Việt Nam; tích hợp mô hình Reranking để tăng độ chính xác khi tìm kiếm ngữ nghĩa; thử nghiệm các mô hình ngôn ngữ lớn chuyên biệt tiếng Việt mã nguồn mở nhằm nâng cao chất lượng câu trả lời và tự chủ dữ liệu.
% % % % \item \textbf{Mở rộng tính năng nghiệp vụ:} Tích hợp công nghệ nhận dạng ký tự quang học (OCR) nâng cao để xử lý các tài liệu quét (scan PDF), hình ảnh pháp lý chất lượng kém từ phía người dùng; xây dựng hệ thống tự động theo dõi và gửi thông báo thay đổi hiệu lực của các văn bản pháp luật liên quan; mở rộng thêm các tính năng soạn thảo văn bản pháp lý tự động.
% % % % \item \textbf{Tối ưu hiệu năng đàm thoại và hạ tầng:} Giảm thiểu tối đa độ trễ của các dịch vụ chuyển đổi giọng nói (STT và TTS) trong phân hệ Voice AI để nâng cao trải nghiệm đàm thoại thời gian thực; tối ưu hóa quy trình tự động co giãn (Auto-scaling) trên cụm Kubernetes nhằm đảm bảo tính sẵn sàng cao khi có lượng lớn người dùng truy cập đồng thời.
% % % % \item \textbf{Đa dạng hóa nền tảng ứng dụng:} Phát triển các phiên bản ứng dụng di động (Mobile Application) trên hai hệ điều hành iOS và Android, giúp người dùng dễ dàng tiếp cận và sử dụng trợ lý pháp luật mọi lúc mọi nơi.
% % % % \end{itemize}
